// This module has been migrated to TypeScript. See `./useFrameHealthMonitor.ts`.
// This shim re-exports the TS module so existing extensionless imports
// (e.g. `import useFrameHealthMonitor from '../hooks/useFrameHealthMonitor'`)
// continue to resolve via Vite's default `.js`-before-`.ts` lookup order.
export { default } from './useFrameHealthMonitor.ts';
