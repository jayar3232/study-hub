// This module has been migrated to TypeScript. See `./api.ts` for the implementation.
// This shim re-exports the TS module so existing `import api from '.../services/api'`
// callers continue to resolve via Vite's default `.js` extension resolution order.
export { default } from './api.ts';
